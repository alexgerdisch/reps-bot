let userRepsCompleted = 0;

function calculateTotalReps(n) {
    console.log(userRepsCompleted += n);
    return userRepsCompleted += n;
}

module.exports = {
    calculateTotalReps,
    userRepsCompleted
}